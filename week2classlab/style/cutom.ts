import { StyleSheet } from "react-native";

const customStyles = StyleSheet.create({
    text: {
        fontSize: 16,
    },
    container: {
        backgroundColor: "#fff",
        flex: 1,
    },
});

export default customStyles;
